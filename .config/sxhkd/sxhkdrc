super + shift + Return
	$TERMINAL

super + {r,p,space}
	dmenu_run 

XF86Audio{LowerVolume,RaiseVolume,Mute}
	pulsevol.sh {minus,plus,mute}

super + control + h
	pulsevol.sh swport

control + alt + p
    firefox -no-remote -private-window -P proxied
